# Soul Engine Configuration
# Copy to .env and fill in your values.
# .env is gitignored — your keys stay private.

# ── LLM Provider (one required) ─────────────────────────
# OpenAI (recommended for server deployment)
# Get your key: https://platform.openai.com/api-keys
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Gemini (alternative, may not work from datacenter IPs)
# Get your key: https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# ── Telegram (optional) ───────────────────────────────
# 1. Talk to @BotFather on Telegram, create a bot
# 2. Copy the token here
# 3. Get your user ID from @userinfobot
TELEGRAM_BOT_TOKEN=
TELEGRAM_OWNER_ID=

# ── Heartbeat Schedule ────────────────────────────────
# Cron format: minute hour day month weekday
# Default: every day at 7:00 AM
HEARTBEAT_CRON=0 7 * * *

# Send heartbeat results to Telegram? (true/false)
TELEGRAM_NOTIFY_HEARTBEAT=false

# ── Impulse System (proactive soul) ─────────────────
# The soul reaches out on its own — spontaneous messages,
# research, server checks, emotions, provocations.
# Requires Telegram to be configured.
SOUL_IMPULSE=true
IMPULSE_MIN_DELAY=600        # Min seconds between impulses (default: 600 = 10min)
IMPULSE_MAX_DELAY=14400      # Max seconds between impulses (default: 14400 = 4h)
IMPULSE_NIGHT_START=23       # Hour to enter quiet mode (default: 23)
IMPULSE_NIGHT_END=7          # Hour to exit quiet mode (default: 7)

# ── Soul App API (optional) ─────────────────────────
# Set an API key to enable the REST + WebSocket API
# The iOS app uses this to connect to your soul
API_KEY=
API_PORT=3001
