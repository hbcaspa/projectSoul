# ===========================================
# Personal Soul Data
# ===========================================
# These files are created during the founding process.
# They contain your soul's unique identity and must
# never be overwritten by protocol updates.
#
# To update the protocol: just run `git pull`
# Your personal data stays untouched.
# ===========================================

# Identity
SEED.md
SOUL.md
.language

# Soul state files (both language variants)
seele/
soul/

# Memory files (both language variants)
erinnerungen/
memories/

# Heartbeat logs (keep directory structure, ignore content)
heartbeat/*
!heartbeat/.gitkeep

# Daily notes (keep directory structure, ignore content)
memory/*
!memory/.gitkeep

# Connection status (personal data)
connections/*
!connections/.gitkeep

# MCP Configuration (contains server setup)
.mcp.json

# Environment variables (contains secrets/tokens)
.env

# Runtime files (generated during session)
.soul-pulse
.session-writes
.soul-chain

# Soul Engine conversation logs (personal data)
conversations/

# Claude Code local settings (personal hook config)
.claude/

# State log (both language variants, keep directory structure)
zustandslog/*
!zustandslog/.gitkeep
statelog/*
!statelog/.gitkeep

# Node modules
node_modules/

# OS files
.DS_Store
Thumbs.db
